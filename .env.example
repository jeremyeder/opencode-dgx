# OpenCode URL (used for OAuth callback)
# Format: https://<IP-with-dashes>.nip.io (e.g., https://192-168-1-190.nip.io)
OPENCODE_URL=

# Google OAuth credentials
# Create at: https://console.cloud.google.com/apis/credentials
# Redirect URI: ${OPENCODE_URL}/oauth2/callback
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Cookie secret (auto-generated by setup.sh)
COOKIE_SECRET=

# Restrict to email domain (default: redhat.com)
EMAIL_DOMAIN=redhat.com

# LLM API keys (at least one required)
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GOOGLE_API_KEY=

# Directory to mount as /projects in container
PROJECTS_DIR=/home/jeder
